import QRCodeContent from './QRCodeContent'

export default async function QRCodePage() {
  // Page autonome accessible à tous (connectés ou non)
  // Utilisée pour le QR code du kakemono au lycée
  return <QRCodeContent />
}
